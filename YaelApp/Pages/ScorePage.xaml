<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:YaelApp.ViewModels"
             xmlns:models="clr-namespace:YaelApp.Models"
             x:Class="YaelApp.Pages.ScorePage"
             Title="Score">
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,20"
            VerticalOptions="Center">

            <Image
                Source="scores_image.jpg"
                SemanticProperties.Description="Image de scores sportifs"
                HeightRequest="200"
                WidthRequest="200"
                Aspect="AspectFit"
                HorizontalOptions="Center" />

            <Label
                Text="Score"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />

            <Label
                Text="Retrouve ici les scores et resultats des matchs."
                FontSize="18"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center" />
        </VerticalStackLayout>
        <VerticalStackLayout Spacing="24" Padding="16">
            <Label Text="Résultats Tennis" FontSize="24" FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding TennisMatches}"
                            SelectionMode="None"
                            Margin="0,0,0,20">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:MatchModel">
                        <Frame Margin="0,6" Padding="12" CornerRadius="12" HasShadow="True">
                                <TapGestureRecognizer
                                    Command="{Binding BindingContext.GoToDetailCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                                    CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Image Grid.Column="0"
                                       Source="{Binding ImageUrl}"
                                       WidthRequest="72"
                                       HeightRequest="72"
                                       Aspect="AspectFill"
                                       VerticalOptions="Center"
                                       HorizontalOptions="Start" />
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                    <Label Text="{Binding NomMatch}" FontSize="20" FontAttributes="Bold" />
                                    <Label Text="{Binding Id, StringFormat='ID : {0}'}" FontSize="12" TextColor="Gray" />
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Résultats Volley" FontSize="24" FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding VolleyMatches}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:MatchModel">
                        <Frame Margin="0,6" Padding="12" CornerRadius="12" HasShadow="True">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding BindingContext.GoToDetailCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                                    CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Image Grid.Column="0"
                                       Source="{Binding ImageUrl}"
                                       WidthRequest="72"
                                       HeightRequest="72"
                                       Aspect="AspectFill"
                                       VerticalOptions="Center"
                                       HorizontalOptions="Start" />
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                    <Label Text="{Binding NomMatch}" FontSize="20" FontAttributes="Bold" />
                                    <Label Text="{Binding Id, StringFormat='ID : {0}'}" FontSize="12" TextColor="Gray" />
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
